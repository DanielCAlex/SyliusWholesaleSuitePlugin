<div class="ui grid">
    {{ form_start(form) }}
    <div class="six wide column">
        {{ form_row(form.name) }}
    </div>
    <div class="six wide column">
        {{ form_row(form.scope) }}
        <p style="color: purple">This ruleset applies to all products in the store.</p>
    </div>
    <div class="twelve wide column">
        {{ form_row(form.description) }}
    </div>
    {{ form_end(form) }}
</div>
