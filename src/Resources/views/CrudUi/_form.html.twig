<div class="ui grid">
    {{ form_start(form) }}
    <div class="eight wide column">
        {{ form_row(form.name) }}
    </div>
    <div class="eight wide column">
        {{ form_row(form.scope)}}
    </div>
    <div class="sixteen wide column">
        {{ form_row(form.description) }}
    </div>
    {{ form_end(form) }}
</div>
