{{ form_errors(form) }}
<div class="ui grid">
    {{ form_start(form) }}
    <div class="eight wide column">
        {{ form_row(form.name) }}
    </div>
    <div class="ten wide column">
        {{ form_row(form.description) }}
    </div>

    <div class="wholesale-rule-tabs sixteen wide column">
        {{ include('@SkyBoundTechSyliusWholesaleSuitePlugin/CrudUi/_ruleTabs,html.twig') }}
    </div>

    <div class="twelve wide column">
        {{ form_row(form.enabled) }}
    </div>
</div>
